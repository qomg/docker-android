# docker with android sdk

## build image

```bash
docker build . --file Dockerfile --tag docker-android:0.0.1
```

## run container

```bash
docker run --name docker-android docker-android:0.0.1
```
